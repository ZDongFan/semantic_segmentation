# SAM AI 编辑运行环境依赖参考。
# create_sam_venv.bat / create_sam_venv.sh 会根据 CUDA 情况先安装 torch/torchvision，
# 再在线安装下面的通用依赖。这里保留为人工排查或自定义安装时的参考清单。
numpy
opencv-contrib-python
Pillow
torch
torchvision
sam2

# SAM1 回退后端需要该依赖；只有使用 sam1 后端时才需要安装。
segment-anything
